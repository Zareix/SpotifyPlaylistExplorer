import logo from "./explorer-logo.png";

const Logo = () => {

  return (
    <img
      src={logo}
      id="AppLogo"
      className="mt-4 mb-2"
      alt="logo"
    />
  );
};

export default Logo;
